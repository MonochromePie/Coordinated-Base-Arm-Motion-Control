[project]
name = "coordinated-base-arm-motion-control"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "MonochromePie", email = "popularmmos2548@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mujoco>=3.13.0",
]

[project.scripts]
coordinated-base-arm-motion-control = "coordinated_base_arm_motion_control:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
